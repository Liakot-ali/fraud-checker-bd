# Secrets & local env
.env
.env.local

# Dependencies (package-lock.json IS committed for reproducible installs)
node_modules/

# OS / editor cruft
.DS_Store

# Logs
*.log
npm-debug.log*

# Tesseract OCR language models (downloaded/cached at runtime)
*.traineddata
*.traineddata.gz
